{
  "name": "Tournament",
  "description": "Tournament web app",
  "main": "index.js",
  "private": true,
  "devDependencies": {
    "browserify": "^6.3.3",
    "envify": "^3.2.0",
    "react": "^0.12.0",
    "reactify": "^0.17.1"
  },
  "scripts": {
    "build": "browserify --debug --transform reactify index.js > bundle.js"
  }
}